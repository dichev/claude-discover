# Agentic Workflow


Reads from `~/.claude/projects/**/*.jsonl`.

## Dev

```
npm install
npm run dev
```

## Production

**macOS:** 
- double-click `start.command` (auto-updates and starts the app)

**Windows:**
```
npm install
npm run build
npm start
```
